# Testing @Shoppify/draggable

## Structure

A simple app with webpack and babel-loader.

## Run

```
npm install

webpack-dev-server
```

You may need to install webpack-web-server globally for simplicity

```npm install webpack-dev-server -g```
